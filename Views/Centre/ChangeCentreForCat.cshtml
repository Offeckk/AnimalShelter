@model AnimalShelter.Models.Cat

@{
    ViewData["Title"] = "ChangeCentreForCat";
}

<h1>ChangeCentreForCat</h1>

<h4>Cat</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="ChangeCenterForCat">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="CentreId" class="control-label">Adoption Centers</label>
                <select asp-for="CentreId" class="form-control" asp-items="ViewBag.CentreId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
